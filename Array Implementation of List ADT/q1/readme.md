Scenario:
In a toll booth system, vehicles are stored in a list (by vehicle number) before being processed.
You need to:

Add new vehicles (create the list).

Insert a new vehicle anywhere in the list (priority vehicle).

Delete a vehicle once it passes the toll (from the front).

Search whether a specific vehicle is in the list.

Display the current list.