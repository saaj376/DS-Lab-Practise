Scenario:
You’re simulating an online shopping cart using an array.
Each product has an integer ID.

Operations:

Create the cart (initial products).

Insert a new product at any position (user adds a product).

Delete a product (user removes it).

Search for a product ID in the cart.

Display the cart contents.